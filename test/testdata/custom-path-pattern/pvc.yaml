apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: test
  name: cache-dir-pvc
spec:
  storageClassName: cache-dir-custom-path-pattern
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
